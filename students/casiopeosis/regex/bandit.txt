6 -> 7
find / -user bandit7 -group bandit6 -size 33c 2>/dev/null
Buscando archivos que tengan usuario bandit7 y grupo bandit6, con tamaño de 33b en el directorio root. 2>/dev/null maneja los errores

7 -> 8
grep "millionth" data.txt
Grep busca la palabra 'millionth' en data.txt en data.txt

8 -> 9
sort data.txt | uniq -u
El comando ordena el contenido de data.txt y usa un pipe para eliminar líneas repetidas con 'uniq -u', -u muestra las líneas únicas.

9 -> 10
strings data.txt | grep "====="
 

10 -> 11
esat data.txt | base64 -d
se lee el documento y se decodifica usando base64 con un pipe.
